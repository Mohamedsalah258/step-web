import { OrdersShell } from './OrdersScreen'

/**
 * فيجما frame: v3-purchase-orders (variant, node 2002:2274) — نفس الشاشة
 * بالتاب «الكل».
 */
export default function OrdersFiltered() {
  return <OrdersShell initialTab="all" />
}
