import { StudentDetailShell } from './StudentDetail'

/**
 * فيجما frame: v3-student-detail — النسخة بدون أوفرلاي (node 35:7124).
 * نفس الشاشة بالحرف ومودال الحظر مقفول لحد ما تدوس «حظر الطالب».
 */
export default function StudentDetailAlt() {
  return <StudentDetailShell />
}
