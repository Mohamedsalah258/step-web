# CLAUDE.md — نقطة البداية

> **اقرأ `WORKFLOW.md` الأول.** فيه الفلو الكامل، الحالة الحالية، القرارات
> المعمارية وأسبابها، خريطة فيجما → الكود، وبروتوكول التعديل.
> **وبعد أي تعديل، حدّثه.**

المشروع: **STEP** — لوحة تحكم إدارية عربية RTL، 62 شاشة محوّلة من فيجما.
React 18 + Vite + TypeScript (strict) + Tailwind 3 + react-router 6.

## ترتيب القراءة

1. **`WORKFLOW.md`** — الفلو · الحالة · القرارات · خريطة فيجما · بروتوكول التعديل
2. **`DESIGN_SYSTEM.md`** — التوكنز · قواعد RTL بالتفصيل · واجهة كل كومبوننت مشترك
3. `README.md` — التشغيل والمسارات

`docs/original-brief.md` هو البريف الأصلي بتاع صاحب المشروع (أرشيف تاريخي).
بعض قواعده اتغيّرت أثناء التنفيذ لأسباب موثّقة في `WORKFLOW.md` قسم 3 —
**اتبع `WORKFLOW.md` مش البريف** لو حصل تعارض.

---

## أربع قواعد ما تكسرهاش

**1. RTL بالكلاسات الفيزيائية.**
في `dir="rtl"` كلاسات تايلويند المنطقية بتنعكس: `text-end` و `justify-end`
و `items-end` معناهم **يسار**. استخدم `text-right` / `text-left`،
و `justify-start` لدفع العناصر لليمين.
وفي أي `flex-row`: **أول عنصر في الـ DOM يظهر على اليمين**.
(`ps-*` / `pe-*` / `ms-*` / `me-*` للـ padding والـ margin شغالين صح — استخدمهم عادي.)

**2. صفر hex في الكود.** كل الألوان والمقاسات توكنز في `tailwind.config.js`.
```
grep -rE "#[0-9a-fA-F]{6}" src/components src/pages   # لازم يرجّع صفر
```

**3. ما تعيدش بناء كومبوننت موجود.** فيه 16 كومبوننت مشترك في
`src/components/ui` + `layout` + `charts`. القايمة والواجهات في `DESIGN_SYSTEM.md`.
لو محتاج حاجة مش موجودة: لو عامة ضيفها في `components/ui`، لو خاصة بدومين
حطها في `pages/<Domain>/<domain>-parts.tsx`.

**4. البيانات في `src/data/` مش في الـ JSX.** مفيش نص عربي أو رقم inline.

---

## قبل ما تقول "خلصت"

```bash
npm run build     # typecheck + build، صفر أخطاء
```
وحدّث سجل التغييرات في آخر `WORKFLOW.md`.
